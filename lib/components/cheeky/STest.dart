import 'package:flutter/material.dart';

// import './../cheeky/HtmlLike.dart';

class SelfTest extends StatelessWidget {
  final String _text;
  final List _listOfOptions;
  final _onCheck;

  SelfTest(this._text, this._listOfOptions, this._onCheck);

  @override
  Widget build(BuildContext context) {
    return Container(
      // child: Radio(value: "one",),
    );
  }
}
